import { createTheme} from '@mui/material/styles'

const theme = createTheme({
    palette: {
      primary: {
        main: '#e47e5d',
        light: '#fff1f1',
        dark: '#542516',
      },
      secondary: {
        main: '#e3c8c8',
      },
    },
  });

  export default theme;